{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "runtimeArgs": [
                "-r",
                "ts-node-dev/register"
            ],
            "args": [
                "${workspaceFolder}/example03.ts"
            ],
            "protocol": "inspector",
            "cwd": "${workspaceRoot}"
        }
    ]
}
